# Screenshots

Place captures in `docs/screenshots/` and they will render directly in the READMEs:

| File | What to capture |
| :--- | :--- |
| `workflow-canvas.png` | The full n8n workflow canvas (both branches + sticky notes) — **already added** |
| `morning-preview.png` | A real morning WhatsApp alert (07:00 CR digest with match kickoff times) |
| `night-preview.png` | A real night results message (23:00 CR digest with scorers) |

**Before committing:**
- Crop out any personal phone numbers or recipient names.
- Suggested size: ~1280px wide PNG for the canvas; ~600px wide for the phone message previews.
- Use a screenshot tool that captures the full message bubble without cutting off content.
